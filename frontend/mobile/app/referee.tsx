import { Trophy } from 'lucide-react-native';

import { StubScreen } from '@/components/StubScreen';

export default function RefereeScreen() {
  return (
    <StubScreen
      title="Espace Juge Arbitre"
      hint="Dashboard organisateur — arrive en Phase 6.2."
      icon={Trophy}
    />
  );
}
